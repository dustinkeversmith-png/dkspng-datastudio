# County comparison module.
# The main callable entrypoint is currently services/r-analysis/analysis_runner.R.

county_compare_notes <- list(
  purpose = "Compare observation counts and metric summaries by county.",
  output = "JSON rows grouped by county."
)
