"""
Adaptive visualization pipeline (sessions, buffer, charts, saves).

The implementation lives in the main API package at the repository root:
``app/workflow/session_pipeline.py`` (:class:`app.workflow.session_pipeline.SessionPipeline`).

Run the API with working directory at the regional-data-studio root so ``import app`` resolves.
"""

__all__: list[str] = []
