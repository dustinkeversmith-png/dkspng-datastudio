from typing import Dict

def parse_research(file_path: str) -> Dict[str, str]:
    return {
        "metric_value": "Primary indicator value extracted from scholarly artifact",
        "year": "Year of publication or observation"
    }
